<%= form_for(@piece, :url => "tagging", :html => { :method => :post }) do |f| %>

   <p>Tags</p>
   <ul>
     <% Tag.all.each do |tag| %>
     <%= hidden_field_tag "piece_tag_ids_none", nil, {:name => "piece[tag_ids][]"}%>
     <li>
       <%= check_box_tag "piece_tag_ids_#{tag.id}", tag.id, (f.object.taggings.present? && f.object.taggings.include?(tag.id)), {:name => "piece[tag_ids][]"} %>
       <%= label_tag "piece_tag_ids_#{tag.id}", tag.name %>
     </li>
     <% end %>
   </ul>

   <%= f.submit %>

<% end %>
