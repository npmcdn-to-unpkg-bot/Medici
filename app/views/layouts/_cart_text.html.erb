<% if number_to_currency current_order.subtotal != 0.00 %>
  <%= link_to "#{current_order.tickets.size} Items in Cart ( #{number_to_currency current_order.subtotal} )", cart_path(id: current_user.id), class: "btn btn-link" %>
<% end %>
